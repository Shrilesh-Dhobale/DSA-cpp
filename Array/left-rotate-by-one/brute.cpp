#include<bits/stdc++.h>
using namespace std;

void solve(int arr[],int n)
{
    int temp[n];
    for(int i=1;i<n;i++)//
    {
        temp[n-1]=arr[i];
    }
    temp[n-1=arr[0]];//first element of original array
    for(int i=0;i<n;i++)
    {
        cout<<arr[i];
    }
    cout<<endl;
}